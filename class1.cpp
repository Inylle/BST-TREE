#include <iostream>
using namespace std;

// Klasa Tree

class Tree {
public:
    int data;
    Tree* left;
    Tree* right;

    // Konstruktor
    Tree(int value) : data(value), left(nullptr), right(nullptr) {}
};

int main() {
    
    

    return 0;
}